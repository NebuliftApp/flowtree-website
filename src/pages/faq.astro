---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import PageHero from '../components/PageHero.astro';
import { faqs } from '../data/faq';

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((faq) => ({ '@type': 'Question', name: faq.question, acceptedAnswer: { '@type': 'Answer', text: faq.answer } })),
};
---

<Layout title="Frequently asked questions — FlowTree" description="Answers about Git worktrees, local-first operation, terminal persistence, AI tools, IDE support, and system requirements.">
  <Navbar />
  <main>
    <PageHero eyebrow="Frequently asked questions" title="Questions about FlowTree." description="Worktrees, local data, terminal sessions, pricing, and system requirements." />
    <section class="section-tight">
      <div class="narrow faq-list">
        {faqs.map((faq) => (
          <details class="faq-item">
            <summary>{faq.question}</summary>
            <div class="faq-answer"><p>{faq.answer}</p></div>
          </details>
        ))}
      </div>
    </section>
  </main>
  <Footer />
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
</Layout>
